use aiken/primitive/string

pub const psetting_nft_tn = string.to_bytearray(@"PROTOCOL_SETTINGS_NFT")

pub const pmanager_nft_tn = string.to_bytearray(@"PROTOCOL_MANAGER_NFT")

pub const project_info_nft_tn = string.to_bytearray(@"PROJECT_INFO_NFT")

pub const project_prediction_nft_tn =
  string.to_bytearray(@"PROJECT_PREDICTION_NFT")
